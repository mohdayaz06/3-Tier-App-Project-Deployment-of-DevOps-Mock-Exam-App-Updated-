apiVersion: v1
kind: Service
metadata:
  name: backend-service
spec:
  selector:
    app: backend
  ports:
    - protocol: TCP
      port: 5000         # Port the service exposes internally
      targetPort: 5000   # Port the backend container listens on
  type: ClusterIP       # Internal service only


